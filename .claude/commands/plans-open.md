Open the Claude Plan Visualizer for this project.

Run the following command in the background (do not wait for it to finish):

```bash
npx claude-plan-visualizer &
```

If the `.claude/plans` or `docs/plans` directory does not exist, create `.claude/plans` first.

Tell the user the viewer is starting at http://localhost:3200.
